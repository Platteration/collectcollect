import { NotFound } from "@collectcollect/core/components/NotFound";

export default function SkinsNotFound() {
  return <NotFound thing="inventory" homeHref="/inventory" homeLabel="Open the inventory" />;
}
