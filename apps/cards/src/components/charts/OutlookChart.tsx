"use client";

// The chart moved to the shared package so a game or a comic gets the same
// outlook; this app's import path is kept.
export { OutlookChart } from "@collectcollect/core/components/OutlookChart";
