# Patterns are matched against the whole path, so anything that can appear
# inside an app as well as at the root needs the ** prefix.
**/node_modules
**/.next
**/data
.git
*.md
.env*
!.env.example
**/tests
**/e2e
**/playwright.config.ts
**/test-results
**/playwright-report
